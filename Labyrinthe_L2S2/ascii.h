#ifndef __ASCII_H__
#define __ASCII_H__

#include "MazeBlock.h"
/*display the maze in ascii-art
This function is not recommended for table with more than 67 columns in full screen*/
void disp_ascii(Maze maze);
#endif